# 🎨 `mindgym_mockup (29).html` 전체 구조 및 화면 요소 상세 분석

본 문서는 프로젝트 루트에 위치한 `mindgym_mockup (29).html` (크기 755KB, 약 6,300 라인) 싱글 HTML 목업 파일의 전체 화면, UI 컴포넌트, 인터랙션, CSS 디자인 토큰 및 자바스크립트 로직을 완벽하게 분석하여 정리한 명세서입니다.

---

## 1. 🔍 목업 개요 및 스타일 토큰

* **디자인 컨셉**: 파스텔 웰니스 (Pastel Wellness) & 무테(Borderless) 카드 레이아웃.
* **폰트 시스템**: Pretendard (Web Font CDN 연동).
* **브랜드 로고**: 이미지 Base64 직접 임베드 (`mg-sym-green`, `mg-sym-white`).
* **컬러 팔레트 (OKLCH 기반)**:
  * Primary Green: `#00C474`
  * Soft Mint Background: `oklch(0.97 0.02 165)`
  * Sage Slate Text: `oklch(0.25 0.03 165)`
  * Muted Card Surfaces: `#F9FAFB` (border 없음)
* **모바일 디바이스 프레임**: `max-w-[430px]`, `mx-auto`, `min-h-screen`, `shadow-xl`, `rounded-[40px]`.

---

## 2. 📱 HTML 내 구현되어 있는 화면 (Screens) 목록

### 2.1 [독립 뷰 1] 온보딩 & KOSS 36문항 진단 스크린 (`#screen-onboarding`)
* **구성 요소를 완전 분석**:
  1. **슬라이드 1~3 (인트로 메인)**: "내 마음의 정원을 가꾸는 시간", "스트릭 강박 없는 따뜻한 누적 캘린더", "나만을 위한 72가지 맞춤 처방" 킬러 카피.
  2. **[나의 마음 진단하기] 터치 액션**: KOSS 직무스트레스 36개 문항 인터랙티브 설문 드로잉.
  3. **프로그레스 바 & 선택지**: 4점 리커트 척도 (전혀 그렇지 않다 ~ 매우 그렇다).
  4. **진단 완료 시**: 8대 영역 점수 자동 산출 후 `🌱 골드 새싹 정원 스킨` 및 웰컴 덤벨 지급.

---

### 2.2 [탭 1] 대시보드 메인 홈 스크린 (`#screen-dashboard`)
* **구성 요소를 완전 분석**:
  1. **상단 프로필 & 이달의 나**: `김민우 님`, `"안정된 8월을 가꾸는 중"`, `[🗓️ 나의 기록]` (탭 4로 이동).
  2. **덤벨 레벨 게이지**: 현재 등급(나무 덤벨 Lv.1)과 다음 등급 진화까지 남은 덤벨 프로그레스 바.
  3. **이달의 마음 정원 (누적 캘린더)**:
     * **실천 완료 타일**: Vercel 스타일 초록 새싹 애니메이션 SVG (`AnimatedSprout`).
     * **자연스러운 휴식 타일**: 따뜻한 찻잔 김 모션 SVG (`AnimatedCoffeeCup`).
     * **하단 다독임 문구**: "하루 쉬어가는 것도 건강한 마음 단련의 일부입니다."
  4. **오늘의 마음 체크인 배너**: 시간대 스캔(오전/오후/저녁) 기반 유동 배너 및 회고 유도 버튼.
  5. **KOSS 맞춤 추천 리추얼 카드**: 취약 영역(예: 관계 갈등) 극복을 위한 1순위 오디오 리추얼 가이드 (`▶ 시작하기`).

---

### 2.3 [탭 2] 자율 리추얼 라이브러리 스크린 (`#screen-ritual`)
* **구성 요소를 완전 분석**:
  1. **검색 바 & 가로 스크롤 필터 칩**: 전체 / 호흡·명상 / 감정 일기 / 힐링 산책 / 수면 회복.
  2. **3열 그리드 카드 배열**:
     * 원형 파스텔 아이콘 + 컴팩트한 타이틀과 리추얼 ID.
     * 72가지 데이터 아이템 나열.
  3. **리추얼 상세 바텀시트**:
     * 아이콘, 실행 시간(3분/7분/15분), 보상 덤벨 수량.
     * 즐겨찾기(`❤️`) 토글 및 `[▶ 시작하기]` (플레이어로 이동).

---

### 2.4 [탭 3] 월간 마음건강 매거진 서재 스크린 (`#screen-magazine`)
* **구성 요소를 완전 분석**:
  1. **이번 달 대표 매거진 배너**: 고해상도 일러스트 커버 & "VOL.11 잠시, 멈춤의 기술".
  2. **과월호 책장 그리드**: 1월호부터 7월호까지 정갈한 아카이브.
  3. **매거진 뷰어 모달**:
     * 아티클 텍스트 & 에디토리얼 이미지 렌더링.
     * 최하단 스크롤 센싱 + 1분 체류 시 `+10 덤벨` 완독 완료 토스트 노출.

---

### 2.5 [탭 4] KOSS 리포트 & 과거 정원 아카이브 스크린 (`#screen-report`)
* **구성 요소를 완전 분석**:
  1. **KOSS 8대 영역 다이어그램**: 8각 레이더 차트 및 종합 정서 상태(🟢안정 / 🟡주의 / 🔴위험) 표시.
  2. **관리 툴킷**: `[이번 달 지향 감정어 변경]` 위저드 모달, `[알림 시간 설정]` 바텀시트.
  3. **과거 마음 정원 컬렉션**: 지난 달 가꾸었던 달력 축소판 및 누적 스탯(총 실천일, 총 완료 리추얼, 명예 배지) 진열장.

---

### 2.6 [독립 뷰 2] 오디오 명상 플레이어 & 정산 오버레이 (`#screen-player`)
* **구성 요소를 완전 분석**:
  1. **자막 싱크 오디오 플레이어**: 재생/일시정지, 프로그레스 슬라이더, 오디오 가이드 자막 하이라이트.
  2. **완료 덤벨 정산 모달**: 플레이 완료 시 획득 덤벨 수치와 함께 당일 마음 정원에 새싹 도장이 날인되는 애니메이션 연출.

---

### 2.7 바텀시트 및 팝업 오버레이 모듈
1. **아침 체크인 바텀시트**: `AnimatedMorningSun` (회전하는 태양) + 감정 칩 선택 + 틈새 처방.
2. **저녁 체크인 바텀시트**: `AnimatedEveningMoon` (숨쉬는 달) + YES/NO 회고 분기 + 영수증 형태의 '오늘의 마음 정원 카드' 저장/공유 기능.
3. **30일 완성 전체화면 골드 팝업**: 30일 누적 완성 시 `골든 가든 스킨`, `+30 덤벨`, `🏆 한 달의 주인공` 칭호 해금.
